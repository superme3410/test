name: Keep Alive

on:
  workflow_dispatch:

  schedule:
    - cron: "*/10 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - run: curl https://6f7db3ufp7egvrin-free-app.stackhost.org
